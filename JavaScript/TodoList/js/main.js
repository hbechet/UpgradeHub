class Todo {

    constructor(todo) {
        this.definition = todo;
    }
}

class List {

    constructor() {
        this.todoList = [];
    }
}

let text = document.querySelector('input');

text.addEventListener('change', event => event.target.value);


let button = documen.querySelector('.btn-add');

button.addEventListener('submit', event => {

});